import { LocalizationResourceName } from "../localization-resource-name.js";

/** Port of `DefaultResource`: the fallback resource named "Default". */
@LocalizationResourceName("Default")
export class DefaultResource {}
